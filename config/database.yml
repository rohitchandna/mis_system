defaults: &defaults
  adapter: sqlite

development:
  database: db/mis_system_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/mis_system_test.db
  <<: *defaults
production:
  database: db/mis_system_production.db
  <<: *defaults
